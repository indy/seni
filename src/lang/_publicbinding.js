class PublicBinding {
  constructor(name, doc, defaults, create) {
    this.name = name;
    this.doc = doc;
    this.defaults = defaults;
    this.create = create;
  }
}

export default PublicBinding;
